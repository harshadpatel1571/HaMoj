@{
    ViewData["Title"] = "Customer List";
}

<partial name="~/Views/Shared/_Breadcrumb.cshtml" />

<div class="col-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Customer</h4>
            <a type="submit" class="btn btn-primary" asp-controller="Customer" asp-action="AddEdit">Add Customer</a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table id="dataGrid" class="display" style="min-width: 845px">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Office No</th>
                            <th>Name</th>
                            <th>Mobile Number</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            columns = [
                { data: "id" },
                { data: "office_No" },
                { data: "name" },
                { data: "mobile" },
                {
                    data: null,
                    render: function (data, type, row) {
                        return `<div class="d-flex">
                                   <a href="/Customer/AddEdit/${row.id}" class="btn btn-primary shadow btn-xs sharp me-1"><i class="fas fa-pencil-alt"></i></a>
                                   <a href="#" class="btn btn-danger shadow btn-xs sharp" onclick="deleteCustomer(${row.id})"><i class="fas fa-trash"></i></a>
                                   </div>`;
                    }
                }
            ];

            BindGrid('/Customer/BindData', columns);
        });

        function deleteCustomer(customerId) {
            Swal.fire({
                title: "Are you sure to delete?",
                text: "You Want To Delete Customer",
                icon: "warning",
                showCancelButton: true,
                confirmButtonColor: "#3085d6",
                cancelButtonColor: '#d33',
                confirmButtonText: "Yes, delete it!!",
                cancelButtonText: "cancel!",
                closeOnConfirm: false
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: `/Customer/Delete/${customerId}`,
                        type: 'POST',
                        success: function (result) {
                            var table = $('#dataGrid').DataTable();
                            table.ajax.reload();
                            toaster('success', 'Record Deleted');
                        },
                    });
                }
            });
        }
    </script>
}

